<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>Edit Shoe</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body>

  <div class="container mt-5">
    <h2 class="text-center mb-4">Edit Shoe</h2>

    <form action="/edit" method="POST" enctype="multipart/form-data">
        <!-- Hidden product ID to be sent in form -->
        <input type="hidden" name="productID" value="<%= shoe.productID %>">
        <input type="hidden" name="currentImage" value="<%= shoe.image %>">

        <div class="mb-3">
            <label for="productName" class="form-label">Shoe Name</label>
            <input type="text" class="form-control" id="productName" name="productName" value="<%= shoe.productName %>" required>
        </div>

        <div class="mb-3">
            <label for="brand" class="form-label">Brand</label>
            <input type="text" class="form-control" id="brand" name="brand" value="<%= shoe.brand %>" required>
        </div>

        <div class="mb-3">
            <label for="size" class="form-label">Size</label>
            <input type="text" class="form-control" id="size" name="size" value="<%= shoe.size %>" required>
        </div>

        <div class="mb-3">
            <label for="price" class="form-label">Price ($)</label>
            <input type="number" step="0.01" class="form-control" id="price" name="price" value="<%= shoe.price %>" required>
        </div>

        <div class="mb-3">
            <label for="quantity" class="form-label">Quantity</label>
            <input type="number" class="form-control" id="quantity" name="quantity" value="<%= shoe.quantity %>" required>
        </div>

        <div class="mb-3">
            <label class="form-label">Current Image</label><br>
            <% if (shoe.image) { %>
                <img src="/images/<%= shoe.image %>" alt="Shoe Image" style="max-height: 200px;">
            <% } else { %>
                <p>No image uploaded.</p>
            <% } %>
        </div>

        <div class="mb-3">
            <label for="image" class="form-label">Upload New Image (optional)</label>
            <input type="file" class="form-control" id="image" name="image" accept="image/*">
        </div>

        <button type="submit" class="btn btn-primary">Update Shoe</button>
        <a href="/shoes" class="btn btn-secondary">Cancel</a>
    </form>
</body>
</html>
